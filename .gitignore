*.pyc
.venv/
__pycache__/
*.py[cod]
.env
.claude/
.env.bak
venv
uploads/
# Documentos jurídicos gerados (§ Contratos) — mesmo padrão de uploads/,
# só o caminho fica no banco, o arquivo em si nunca é versionado.
gerados/

.env.anterior-*
